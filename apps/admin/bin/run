#!/usr/bin/env node

const oclif = require("@oclif/core");

process.env.API_PATH = "../api";
process.env.WEB_PATH = "../web";

oclif
	.run()
	.then(require("@oclif/core/flush"))
	.catch(require("@oclif/core/handle"));
